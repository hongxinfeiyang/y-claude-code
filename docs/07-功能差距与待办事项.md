# 07 — 功能差距与待办事项

## 1. 与 Claude Code 差距总览

本文档记录 y-claude-code 与真实 Claude Code 之间的功能差距，按优先级排列待办事项。

对比基准日期：2026-06-04。

## 2. 核心能力差距（P0 — 影响 Agent 行为质量）

### 2.1 System Prompt 增强

- [x] 编写完整的 system prompt，覆盖：
  - [x] 工具选择决策树（何时用 Read vs Grep vs Glob）
  - [x] 代码修改规范（编辑前先读取、Edit 优于 Write+Bash sed）
  - [x] 安全约束（绝不跳过 hooks、危险命令识别、敏感文件保护）
  - [x] Git 操作规范（新 commit 而非 amend、不跳过 hooks）
  - [x] 输出风格约束（简洁、不写冗余注释、无 emoji 除非用户要求）
  - [x] Token 预算意识（告知当前窗口大小和使用率）
  - [x] 子代理使用指导（何时委派 vs 自己做）
- [x] 环境信息注入（platform、os version、shell、workspace 路径等）
- [x] 当前日期注入（让 Agent 知道"今天是什么日期"）

### 2.2 Plan Mode（计划模式）

- [x] 实现 `EnterPlanMode` 流程
  - [x] plan mode 下的工具限制机制（只读工具过滤）
  - [x] plan 内容传递（ExitPlanMode plan 参数）
  - [x] `ExitPlanMode` 审批流程（回调注入模式）
- [x] CLI 中 plan mode 的交互 UI（审批回调的终端实现 — askYesNo + 计划内容 Markdown 渲染）
- [x] Agent Loop 对 plan mode 的状态感知（PLANNING 状态 + 工具过滤）

### 2.3 Thinking 展示

- [x] CLI 终端中 thinking 内容可折叠展示（ANSI 变暗 + `/thinking` 切换）
- [x] extended thinking token 配置可调（thinkingTokens 配置项）
- [x] 用户可开关 thinking 展示（`showThinking` 配置 + `Y_CLAUDE_CODE_SHOW_THINKING` 环境变量）

### 2.4 子代理系统完善

- [x] 子代理类型定义（Explore / Plan / general-purpose (claude) / code-reviewer）
- [x] 子代理隔离级别（worktree / process / none — worktree 标记为待实现）
- [x] AgentTool 实际实现（AgentLoop 驱动的完整子代理 + 环境信息注入）
- [x] 子代理并发执行（executeSubAgentsInParallel + Promise.all）
- [x] 子代理结果汇总与返回（结构化输出 + 工具统计）

## 3. 上下文管理差距（P1 — 影响长对话质量）

### 3.1 LLM 驱动上下文压缩

- [x] 用 LLM 调用实现渐进式摘要（当前是纯文本截断）— Summarizer 已实现
  - [x] 分块摘要: 旧消息按 20 条/块分组，每块独立 LLM 摘要后合并
  - [x] 渐进式合并: 已有摘要 + 新消息 → LLM → 合并摘要
  - [x] 摘要自压缩: 摘要超过 8000 tokens 时自动压缩
  - [x] 失败降级: LLM 摘要失败时自动回退文本截断
  - [x] ContextBuilder.compressHistory() 优先走 Summarizer
  - [x] AgentLoop.compactMessages() 使用真实摘要替代占位符
- [x] 压缩触发阈值可配置（threshold: 0.70, chunkSize, recentPreserveCount 等）
- [x] 旧消息保留策略（最近 N 条原样保留 + 旧消息 LLM 摘要）

### 3.2 Prompt Cache 完善

- [x] 缓存断点自动标记（system prompt + Anthropic provider 自动添加 cache_control）
- [x] 缓存命中率统计与展示（CacheManager + API usage 提取 + getStatsSummary()）
- [x] 缓存 TTL 感知（5 分钟窗口 + shouldRefresh() + getRemainingTTL()）

### 3.3 上下文窗口实时监控

- [x] CLI 状态显示 token 使用率（ContextMonitor.getStatusBarText() + context_alert 事件）
- [x] 接近上限时主动警告（三级告警: warning 70% / critical 85% / danger 95%）
- [x] 自动触发压缩的建议提示（shouldCompact + getAlert() 建议 /compact）

## 4. 工具系统差距（P1 — 影响操作能力）

### 4.1 缺失工具

- [x] **TodoWrite** — 结构化任务列表管理（pending/in_progress/completed 三态）
- [x] **NotebookEdit** — Jupyter Notebook .ipynb 单元格编辑（replace/insert/delete）
- [x] **EnterPlanMode / ExitPlanMode** — 已在 P0-2.2 实现
- [x] **EnterWorktree / ExitWorktree** — 工作树隔离（已实现，含创建/进入/保留/删除等完整生命周期）
- [x] **TaskOutput** — 获取后台任务输出
- [x] **TaskStop** — 停止后台任务
- [x] **Skill** — 触发项目级 Skill 调用
- [x] **CronCreate / CronDelete / CronList** — 定时任务管理（创建/删除/列举）
- [x] **ScheduleWakeup** — 动态调度唤醒（延迟指定秒数后继续任务）

### 4.2 现有工具增强

- [x] **Read** — 支持图片读取（PNG/JPG）+ PDF 文本提取（pdf-parse 集成，支持页码范围）+ Jupyter Notebook 格式化读取（单元格 + 输出展示）
- [x] **Edit** — 替换后展示彩色 unified diff（ANSI 颜色标记 +/- 行，3 行上下文）
- [x] **Bash** — 终端 ANSI 转义序列处理（自动清除颜色码/控制码）
- [x] **Bash** — 完整的后台任务生命周期管理（taskId 注册 + TaskOutput/TaskStop 集成）
- [x] **WebFetch** — HTML 到 Markdown 转换（标题/链接/代码块/列表/引用/图片全支持）
- [x] **WebSearch** — 搜索结果去重（URL + 标题 Jaccard 相似度）+ 相关性排序（词/短语匹配加权）

## 5. 交互体验差距（P2 — 影响易用性）

### 5.1 CLI 终端体验

- [x] 彩色 diff 展示（Edit 操作后，ANSI 红/绿标记）
- [x] 进度指示器（Agent 执行中显示旋转 spinner 动画）
- [x] 状态栏（启动时显示模型、Provider、会话 ID）
- [x] Markdown 渲染（代码块语法高亮 + 粗体/行内代码/标题 ANSI 着色）
- [x] 历史命令搜索（Ctrl+R，readline 原生支持）
- [x] 多行输入支持（行尾 \\ 续行）
- [x] tmux 会话集成（TmuxManager + 环境检测 + /tmux 命令 + 窗口/面板管理 + 启动信息展示）

### 5.2 斜杠命令扩展

- [x] `/compact` — 手动触发上下文压缩
- [x] `/remember` — 手动保存记忆
- [x] `/tasks` — 查看当前任务列表（对接 TodoWriteTool.getTodos()）
- [x] `/hooks` — 查看 hooks 配置
- [x] `/stats` — 查看会话统计
- [x] `/statusline` — 配置状态栏
- [x] `/add-dir` — 添加工作目录
- [x] `/doctor` — 诊断环境（Node/平台/Shell/Provider/模型/配置路径）
- [x] `/pr-comments` — 查看 PR 评论
- [x] `/context` — 查看上下文使用情况

### 5.3 VS Code 插件完善

- [x] 真正的内联补全（对接 LLM，通过 `provideCompletion` 命令调用 core Provider）
- [x] Diff 视图（Webview 内渲染 +/- 差异 + `vscode.diff` 原生命令）
- [x] 文件 Apply 按钮（一键应用 AI 建议的编辑）
- [x] 终端面板联动（插件 ↔ CLI 互通，通过 `processMessage` 命令 + AgentLoop 流式输出）
- [x] 设置页面（图形化配置 provider/model/apiKey/maxToolRounds/showThinking）
- [x] 右键菜单集成（编辑器上下文菜单: 解释选中代码 + 审查文件）

## 6. 工程基础设施差距（P2 — 影响稳定性和安全性）

### 6.1 安全增强

- [x] Prompt Injection 多层防护（输入层 10 种攻击模式检测 + LLM 输出过滤层）
- [x] 输出脱敏（API Key/Token/密码/JWT/AWS Key/DB 连接串/GitHub Token 等 9 种敏感信息正则脱敏）
- [x] SSRF 防护（WebFetch 中内网地址拦截含 link-local/IPv6 + DNS rebinding 防护）
- [x] 命令注入深度检测（Bash 中 6 种危险模式正则检测 + 沙箱隔离）

### 6.2 测试完善

- [x] Agent Loop 完整集成测试（13 个用例：文本对话、工具循环、停止条件、错误处理、状态、消息历史、thinking、空输入、连续运行）
- [x] 工具组合场景测试（6 个用例：Read→Edit 管道、Bash→Read 管道、Grep→Read 管道、Glob→Read 管道、路径穿越防护、分段读取）
- [x] LLM Mock 测试（8 个用例：文本 chunk、tool_use、error、多轮、空响应、thinking、上下文窗口、token 计数）
- [x] 权限系统边界测试（18 个用例：默认模式、规则匹配、优先级、缓存、回调、边界场景）
- [x] Docker 沙箱安全测试（42 个用例：命令拦截 8 模式、挂载校验 9 场景、策略结构、Docker 生命周期、安全策略集成 6 项、超时控制、降级行为 3 项、边界场景 5 项）
- [x] VS Code 插件测试（63 个用例：completion 34 + panel 19 + extension 10，覆盖命令注册、语言/内容过滤、流式缓冲、消息路由、状态栏等）
- [x] 上下文摘要器测试（21 个用例：阈值判断、首次分块摘要、LLM 失败降级、状态管理、消息分块、消息转文本、降级文本格式）
- [x] Anthropic Provider 测试（16 个用例：contextWindow、supportsFeature、countTokens、构造）
- [x] Bash 工具测试（18 个用例：元数据、危险模式检测 6 种、ANSI 清除、后台任务管理）
- [x] 配置加载器测试（30 个用例：默认值、set/get/getKey、Provider 管理、环境变量覆盖、load 多级加载、单例）
- [x] 技能加载器测试（25 个用例：loadFromDirectory、frontmatter 解析、同名覆盖、loadAll、get/listAll/listNames、buildSystemPromptSection、parseSimpleYaml 边界）
- [x] CLI 斜杠命令测试（38 个用例：注册、别名、执行、内置命令 20 个全覆盖含 /tmux）
- [x] CLI 诊断测试（19 个用例：diagnoseConfig 12 场景、formatDiagnostics、runDiagnostics）
- [x] 测试夹具目录（fixtures：configs/valid-settings.json、llm-responses/simple-response.jsonl、projects/sample-app/）
- [x] 全量测试：25 个文件 409 个用例全部通过

### 6.3 可观测性 — 已设计并实现

- [x] JSONL Transcript 写入（完整对话记录）— TranscriptWriter 已实现
  - [x] 8 种事件类型: turn:start/end, llm:call/result, tool:call/result, error, recovery
  - [x] 写入 ~/.y-claude-code/transcripts/{sessionId}.jsonl
  - [x] 每行一个 JSON 对象，可流式解析
- [x] 结构化日志增强 — ObservabilityManager 统一入口
- [x] 性能指标收集 — MetricsCollector 已实现
  - [x] Counter: llm.calls, tool.calls, recovery (按标签分组)
  - [x] Histogram: llm.latency, tool.latency, token.usage (带分桶)
  - [x] Gauge: session.token.total, session.tool.rounds, session.messages.count
- [x] 链路追踪 — Tracer 已实现
  - [x] Span 树: agent.turn → llm.call / tool.execute:{name}
  - [x] 自动父子关系推断（活跃 Span 栈）
  - [x] 耗时统计、状态标记、元数据附加
- [x] Agent Loop 集成 — 7 个注入点全部接入
  - [x] run() 入口/出口: startTurn / endTurn
  - [x] LLM 调用前后: recordLLMCall / recordLLMResult
  - [x] 工具调用前后: recordToolCall / recordToolResult
  - [x] 错误与恢复: recordError / recordRecovery

### 6.4 其他

- [x] Auto-update CLI 自动更新（AutoUpdateManager + npm registry 检查 + 24h 节流 + 启动时异步通知）
- [x] Telemetry（用户可选择关闭 — TelemetryManager + 匿名安装 ID + 本地存储 + 环境变量/配置开关）
- [x] CLI 终端渲染增强
  - [x] 工具调用展示具体参数（文件路径、命令、搜索模式等，60 字符截断）
  - [x] 权限确认自动记忆（y=本次+记忆同操作 / a=本会话全允许此类操作）
  - [x] 权限缓存细粒度 key（Bash:cmd基名、Write/Edit:file_path）
  - [x] 行清除 writeLine（readline.clearLine + cursorTo 替代裸 \r，消除 spinner 残留空白区）
  - [x] 空白行压缩（StreamingMarkdownRenderer 跨 chunk 追踪，最多保留 2 连续空行）
- [x] VS Code Webview 工具审批增强
  - [x] 工具调用展示文件路径/命令/关键词等关键参数
  - [x] Accept/Reject 按钮（居右对齐）
  - [x] 审批回调通过 postMessage 异步通信
- [x] 错误恢复与优雅降级策略（架构设计已完成，代码已实现）
  - [x] 错误分类体系（11 种错误类别，LLM/工具/系统三层）
  - [x] 指数退避重试（最大 3 次，1s→30s 上限）
  - [x] Provider 回退链（DeepSeek → Anthropic → OpenAI）
  - [x] 熔断器状态机（CLOSED → OPEN → HALF_OPEN）
  - [x] CONTEXT_OVERFLOW 自动压缩后重试
  - [x] ErrorRecovery 代码实现
- [x] settings.json vs settings.local.json 层级完善（4 级合并: 默认值 < 用户全局 < settings.json < settings.local.json < 环境变量）

- [x] 配置合并深度递归（深层配置字段不会在合并时被意外覆盖）
- [x] 代码质量修复 8 项（规划闸门解耦、Token 估算优化、Bash 权限增强、配置深度合并、恢复计数修复、路径规范化、注册表 replace、阈值可配置），详见 [08-01-代码质量修复记录.md](08-01-代码质量修复记录.md)
- [x] 架构重构 7 项（AgentLoop 职责拆分、AgentLoopContext 拆分、错误恢复统一、状态机规范化、PlanMode 解耦、权限 DRY 消除、中间件管道），详见 [08-02-架构重构记录.md](08-02-架构重构记录.md)
- [x] 架构优化补充 3 项（services 迁移、LLMCallManager 提取、ToolExecutor 提取），详见 [08-03-架构优化补充记录.md](08-03-架构优化补充记录.md)

## 7. 已完成事项

- [x] ReAct Agent Loop — 推理 → 工具调用 → 观察 → 再推理
- [x] 10 个内置工具 — Read/Write/Edit/Bash/Glob/Grep/WebFetch/WebSearch/Agent/AskUserQuestion
- [x] LLM Provider 双后端 — Anthropic + OpenAI
- [x] 意图路由 — 斜杠命令 / 直接工具调用 / 自然语言三路分发
- [x] Docker 沙箱 — 容器生命周期管理 + 安全策略 + 降级
- [x] 权限系统 — 五级权限 + 规则引擎
- [x] Token 预算管理 — 比例分配 + 文本级历史压缩
- [x] MCP 协议 — JSON-RPC 客户端 + Tool 适配器
- [x] Hook 系统 — 生命周期事件钩子框架
- [x] Skill 系统 — Markdown 加载 + frontmatter + 三级优先级覆盖
- [x] Memory 系统 — 用户/项目记忆持久化
- [x] Cron 调度 — 定时任务 + 持久化
- [x] Session 管理 — 文件存储 + 会话恢复
- [x] System Prompt 增强 — 完整角色定义 + 环境信息注入 + 工具选择指导 + 安全/Git/代码规范
- [x] Plan Mode — EnterPlanMode / ExitPlanMode 工具 + PLANNING 状态 + 只读工具过滤
- [x] Thinking 展示 — CLI 变暗显示 + `/thinking` 切换 + `showThinking` 配置
- [x] 子代理系统 — 4 种类型 + 隔离级别 + 环境注入 + 并发执行 (Promise.all)
- [x] CLI 基础 — readline 交互 + 7 个内置命令
- [x] VS Code 扩展骨架 — 聊天 Webview + 命令注册
- [x] 工具增强 (P1-4.2) — Read(PDF/Notebook) + Edit(彩色diff) + Bash(ANSI清理+后台任务) + WebFetch(HTML→Markdown) + WebSearch(去重+排序)
- [x] CLI 终端体验 (P2-5.1) — 进度指示器(spinner) + 状态栏 + Markdown渲染(代码高亮) + 历史搜索(Ctrl+R) + 多行输入(\\续行)
- [x] 斜杠命令扩展 (P2-5.2) — /compact /remember /tasks /hooks /stats /statusline /add-dir /doctor /pr-comments /context 共 10 个命令
- [x] 安全增强 (P2-6.1) — Prompt Injection(10 种模式) + 输出脱敏(9 类敏感信息) + SSRF(link-local/IPv6/DNS rebinding) + 命令注入检测(6 种模式)
- [x] VS Code 插件完善 (P2-5.3) — 内联补全(对接LLM) + Diff视图(webview+原生) + Apply编辑 + 流式AgentLoop + 设置页面(6项配置) + 右键菜单
- [x] 测试完善 (P2-6.2 部分) — Agent Loop 集成(13) + 工具组合(6) + LLM Mock(8) + 权限边界(7) + 安全/路由/注册表修复，共 14 个测试文件 137 个用例全通过
- [x] CLI Plan Mode 交互审批 (P0-2.2) — askYesNo 终端提示 + 计划 Markdown 渲染 + 工具权限审批回调
- [x] EnterWorktree/ExitWorktree (P1-4.1) — 完整 git worktree 生命周期（创建/进入/保留/删除）+ AgentTool 隔离指引更新
- [x] Auto-update 自动更新 (P2-6.4) — AutoUpdateManager + npm registry 检查 + 24h 节流
- [x] Telemetry 遥测 (P2-6.4) — TelemetryManager + 匿名安装 ID + 本地 JSONL 存储 + 环境变量开关
- [x] 测试补齐 (P2-6.2 全部完成) — 25 个测试文件 409 个用例：
  - 上下文摘要器(21) + Anthropic Provider(16) + Bash 工具(18) + 配置加载器(30) + 技能加载器(25) + CLI 斜杠命令(38) + CLI 诊断(19) = 新增 7 个文件 167 个用例
  - 测试夹具目录(fixtures)：configs、llm-responses、projects
- [x] Docker 沙箱安全测试 (P2-6.2) — 42 个用例覆盖安全策略校验、Docker 生命周期、安全拦截集成、超时控制、降级行为
- [x] VS Code 插件测试 (P2-6.2) — 63 个用例覆盖补全过滤、面板消息路由、命令注册、流式缓冲
- [x] CLI 终端渲染增强 — 工具调用参数展示、空白行压缩、writeLine 行清除、权限细粒度缓存 key、spinner 集成
- [x] VS Code Webview 工具审批 — Accept/Reject 按钮、工具调用参数展示、postMessage 异步审批流
