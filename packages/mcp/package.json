{
    "name": "@y-claude-code/mcp",
    "version": "0.1.0",
    "description": "MCP 协议支持 — Model Context Protocol 客户端与服务端实现",
    "type": "module",
    "main": "./dist/index.js",
    "scripts": {
        "build": "tsup src/index.ts --format esm --dts --clean",
        "dev": "tsup src/index.ts --format esm --dts --watch",
        "typecheck": "tsc --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.0.0",
        "@y-claude-code/core": "workspace:*"
    },
    "devDependencies": {
        "@types/node": "^25.9.1",
        "tsup": "^8.3.0",
        "typescript": "^5.6.0",
        "vitest": "^2.1.0"
    }
}
